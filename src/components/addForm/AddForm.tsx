import React from "react";
import classes from "./AddForm.module.scss";

const AddForm = () => {
  return (
    <>
      <div className={classes.container}>
        <p className={classes.title}>Fill all fields to continue</p>
      </div>
    </>
  );
};

export default AddForm;
